# Local V2 verification — brain revision

## Open operation timeline — 21 September 2026

The tabbed six-panel example is replaced by four visible milestones: Firm Memory, proposed time, professional review and logged time. A noninteractive copy of the projected brain introduces the information inputs. The illustrative week is internally consistent: 38.5 hours proposed = 34.0 logged + 4.5 awaiting review. Figures are labelled illustrative and scoped to one professional/week; they are not customer results.

Playwright CLI review covered 1440×1000, 786×994 and 390×844. Screenshots verified the horizontal desktop and vertical smaller-screen timelines, no cards, readable KPIs, and no horizontal overflow. All four milestones remain visible; the miniature contains 176 points and 219 links with an accessible image description. The Work to Invoice enquiry selection and Escape dismissal passed, with no submission. Browser console reported zero errors/warnings. The static HTML contains all copy/KPIs plus a brain outline fallback; this fallback was source-reviewed, not separately browser-emulated. The Mac was locked, so the native Codex preview could not be inspected; validation used an isolated local Playwright browser.

Source validation passes 26 references across six V2 files and four scripts. Original tracked files remain unchanged. Evidence: `output/playwright/time-journey-desktop.png`, `time-journey-tablet.png`, and `time-journey-mobile.png`.

## Visible phase examples — 21 September 2026

Two annotations now follow the client-meeting/CRM/Lead to Matter and client-call/Time Capture/Work to Invoice stories across all four phases. They update on the existing phase event, point to real diagram targets, and use finite entrance transitions. Their leaders follow target reveal opacity, so they do not point at an invisible agent or operation during construction. Reduced motion skips the entrance animation.

Fresh browser review covered the natural 786×994 pane, 1280×720, 390×844 and 390×480. Both examples fit; compact screens place them side by side below a slightly smaller diagram. Memory cards remain within the mobile stage without horizontal overflow. PageDown/PageUp confirmed that scroll changes the examples in both directions. Time Capture detail still opens, temporarily hides the annotations and dismisses with Escape. One diagram tab stop remains and no browser errors were reported. Source validation passes 38 references across five files and three scripts; original tracked files remain unchanged. Reduced motion was reviewed in source rather than browser emulation.

## Anatomical brain shape — 21 September 2026

Replaced the oval lobes and latitude rings with wider upper hemispheres, a narrower base, a continuous medial cleft, scalloped cortex outlines and 18 smooth branching folds. The folds follow the same projected surface as the points and appear with Firm Memory. Original point coordinates remain separate lookup keys for semantic links and agent contacts.

Fresh visual checks at 786×994 and 390×844 covered Firm Memory, Work Signals and the complete operations view. The diagram and mobile callout fit without horizontal overflow. All 176 signal hit targets align with their projected points, all SVG paths are finite, 219 links and one roving tab stop remain. Opening a memory point still shows its detail and highlights two neighbouring links; Escape dismisses it. No browser errors were reported. Independent source review confirmed all link/contact identities and finite geometry across the camera range. Source validation passes 38 references across five V2 files and three scripts; original tracked files remain unchanged.

## 3D brain — 21 September 2026

Browser visual review covered 1280×720, the user's 786×994 pane, and 390×844. The four stages retain 176 points, 219 semantic connections, eight agents and two operation groups. Every projected hit target matches a visible signal coordinate, all link coordinates are finite, and mobile points fit inside the graphic without horizontal overflow. All 219 link endpoints and eight agent contacts also resolve in an independent source review.

Verified memory-point detail and neighbouring-link highlights after projection, spatial arrow navigation with one tab stop, Escape dismissal, Time Capture agent detail, and the four-agent Work to Invoice group. The mobile tooltip stays within the stage. A native pointer drag across empty diagram space changed the projected camera coordinates. No browser errors were reported. Reduced-motion behavior and termination of the render loop were reviewed in source, without a separate emulated-media browser run.

Source validation passes 38 references across five V2 files and three scripts. Original tracked files remain unchanged; the loopback preview was restarted, with nothing deployed.

## Messaging pivot — 18 September 2026

Reviewed the hero, brain stages, Managed Operations, Time Capture example, control section, dialogs and metadata against the three value pillars supplied by the user. An independent copy review confirmed the memory asset, prebuilt-agent productivity/records benefit and visibility/access distinction, and checked matching operation descriptions across the catalogue and brain.

Browser review covered the new hero at 390×844 and 1280×900, mobile control/closing layout, desktop operation framing, and the memory/control dialog content. No horizontal overflow or browser errors were observed. Source validation passes 34 references and both scripts. The changes are copy-led, with small layout adjustments for text; the existing brain and six-step interactions remain. Original tracked files have no diff.

## Concrete Time Capture example — 18 September 2026

Compared the live oakbase.ai “04 / EXAMPLE” with the preserved original homepage and retained its six step descriptions and integration marks inside Managed Operations. The abstract example and operation orbit are removed; the page now has four main sections.

Native-browser review at 1280×900, 766×863 and 390×844 found no horizontal overflow. At the user's 766px pane, all six selections displayed the matching panel and loaded logos, with a constant 452px panel height. Home and Next step navigation, focus transfer and the single tab stop passed. The final action opened the local enquiry with Work to Invoice selected. The expanded catalogue still switched between law and wealth operations. Mobile uses horizontal navigation with a 3×2 arrangement; the draft output remained legible. No browser errors were reported. All descriptions are present in static HTML as a no-JavaScript fallback; that fallback was checked in source, not in a separate browser run.

Source validation passes 34 references across four V2 files and two scripts. Original tracked files have no diff. No deployment or external submission occurred.

## Larger diagram and callout — 18 September 2026

Native-browser visual review covered 1280×720, the user's 766×863 pane and 390×844. The wider diagram and connected callout fit without obscuring the points. At 766×863, the completed operations fit, navigation reports horizontal orientation and the page has no horizontal overflow. Opening a point hides the callout; Escape restores it; advancing to Operations hides it. No browser errors were reported. Source validation passes 22 references across four V2 files and both scripts. The original tracked files remain unchanged.

## Contextual hover, touch and keyboard exploration — 17 September 2026

This revision replaces the nine-agent/three-operation diagram with eight agents in two coherent four-agent operations: Lead to Matter and Work to Invoice. All 176 work-signal points resolve to linked signal, memory, agent and operation details according to the active stage.

All 16 focused checks passed at 1440×900 and touch-enabled 390×844: stage-specific content, matching agent and operation, four correct agents per sampled operation, group highlighting, unrevealed-target exclusion, Escape and pointer-out dismissal, phase-change dismissal, one roving Tab stop, spatial arrow navigation, tap/tap-again/outside dismissal and mobile tooltip bounds. No JavaScript errors occurred. Root visually reviewed desktop signal/operation cards and the mobile card. A native-browser check also confirmed that the Work to Invoice catalog dialog lists its four agents and carries the same operation into the local enquiry selector. Final source validation checks 22 references across four V2 files and both scripts.

Evidence: `output/playwright/brain-hover-results.json` and `brain-hover-*.png`. Representative meanings were sampled rather than all 176 repeated point mappings. The additional nearest-point fallback for touches outside a direct target was not separately exercised in the automated run. Historical whole-page and no-JavaScript checks below predate this hover revision.

## Brain network — 17 September 2026

The current brain diagram supersedes the historical plate designs below. All 17 focused checks passed at 1440×900, 649×863, 390×844 and 390×480. Native wheel input verified cumulative construction and reversal: 176 signal dots, 219 memory links, nine agents, then three operation groups containing three agents each. Tab clicks and keyboard navigation also passed.

No overflow, completed-group clipping, orphan groups, script errors, failed requests or external requests were found. Fresh loads remain at the top. Reduced motion shows the complete static graphic. The JavaScript-disabled check covered the initial static fallback and screenshot; animation timing was intentionally not asserted in that mode. The report records the screenshot automation workaround.

The final 390×844 stage is 570px tall; automated checks and the parent’s native-browser review confirm all three groups fit. Desktop, 649px, short-mobile and no-JavaScript screenshots were also reviewed. Local sampled frame timing reached a worst per-sample p95 of 16.8ms, with no observed long tasks. This is not a production benchmark. Source validation passed 21 references; the tracked original-site diff remains empty.

Results: `output/playwright/brain-results.json`. Screenshots: `brain-1440x900-phase-3.png`, `brain-1440x900-phase-1-5.png`, `brain-390x844-phase-3.png`, `brain-649x863-phase-3.png`, `brain-390x480-phase-3.png`, `brain-reducedMotion-649x863.png` and `brain-noJavaScript-649x863.png`, all under `output/playwright/`.

## Refined surfaces and scroll construction — 17 September 2026

This update supersedes the earlier plate styling, 20-unit offsets, 80svh runway and short-height static fallback. The graphic now uses softly lit thin plates without any text on their faces. Names and brief explanations sit outside the scene. The 180svh runway gives each arrival a clearer scroll interval; plates settle at 0/-10/-20/-30 with a 300-unit approach.

All 16 focused checks passed at 649×863 (the observed user pane), 1280×650, 390×844 and 390×480. Native wheel steps build all four layers and reverse them; the counter follows 01/04 through 04/04. Fresh loads do not scroll automatically. Short windows retain construction, and the 390×480 scene fits with its bottom at y456. Only reduced motion selects the static stack. No text elements remain inside the plates. No overflow, script errors, failed requests or external requests were found.

Visual inspection additionally covered a real wheel-driven arrival in the 649×863 pane and the desktop composition. Incoming plates are clipped to the illustration region to avoid crossing the external text. Handoff starts at Work Signals. Source validator passes 24 references across three source files. Evidence: `output/playwright/refined-layer-results.json`.

## Compact plate landing — 17 September 2026

The four plates now share an identical footprint and settle with touching edges at offsets 0/-20/-40/-60. Focused desktop (1440×900) and mobile (390×844) checks verified each plate’s continuous downward movement from 250 units above its final position, full opacity on landing, and the reverse movement when scrolling upwards. The whole stack stays stationary; only the arriving layer moves. Reduced motion uses the completed compact stack. No overflow or JavaScript errors were found.

Visual review covered the completed stack on both viewports and an intermediate frame with Agents descending onto the settled Work Signals/Firm Memory stack. The original site remains untouched. Source validation passes. Evidence: `output/playwright/stack-landing-results.json`.

## Four-layer scroll update — 17 September 2026

The central graphic now builds **Work Signals → Firm Memory → Agents → Managed Operations** from scroll position. There is no Play flow control, playback timer or automatic loop. Selecting a layer navigates to its point in the short sticky sequence.

Fresh responsive checks at 1440×900, 1024×768, 390×844 and 375×812 verified the forward and reverse sequence, no overflow, and a scene that fits beneath the header. The pinned stage ends at y784, y744, y780 and y780 respectively, within each viewport. Short viewports and reduced motion reveal all four layers without the pinned sequence. Zero JavaScript errors or failed requests were observed.

Desktop and mobile visual inspection confirmed the new Work Signals base, all four readable labels, progressive layer assembly and synchronised descriptions. Hidden layers do not intercept clicks. The original tracked homepage and deployment files still have no diff. Source validation passes with 22 references.

All 15 focused checks passed, including settled click/keyboard navigation, SVG selection, the Work Signals dialog, hidden-plane pointer exclusion and JavaScript-disabled fallback. No external requests or playback timers occurred. Evidence: `output/playwright/four-layer-results.json`.

The earlier five-section verification follows; its three-layer visual has been superseded by this update.

Verified 17 September 2026 after the five-section redesign.

## Source and isolation

`python3 tools/validate_v2.py` passes: 22 local asset/fragment references across three V2 source files, no duplicate IDs, and valid JavaScript syntax. There is no frontend build step. Original tracked site, assets, legal and deployment files have no diff against baseline `69355aa87c7865c96f2cdf96318ae141a7eb9dd1`. Nothing was pushed or deployed.

## Responsive and visual review

Fresh Playwright checks passed at actual viewports 1440×1000, 1024×768, 390×844 and 375×812. No horizontal overflow, JavaScript errors, failed requests or external requests were found. All three HTML images and the used local fonts loaded.

Codex browser visual inspection covered the desktop hero, three-layer architecture and operation selector; the mobile hero, full architecture, evidence example and memory dialog; and the user’s natural preview-panel width. SVG text follows the plane angles without crossing the plane edges. Section anchors account for the sticky header.

The default main story has approximately 230 rendered words (viewport-dependent), with five narrative sections. Supporting detail is shown only after interaction. The twelve-section initial version and its QA record are archived in `docs/archive/v2-initial/`.

## Interaction and accessibility

- Layer tabs update the selected visual, short description and detail link. Arrow keys, Home and End work. ARIA orientation matches the vertical desktop and horizontal mobile layout.
- Evidence details distinguish observed records from interpretation. Escape closes the native dialog and restores focus to its trigger.
- Law operations show three choices, expanding to six; wealth shows three, expanding to five. Operation rows behave as an accordion.
- Selecting Client Onboarding and choosing **Assess this operation** opens the enquiry with the correct selection.
- Submitting illustrative form data produces the explicit local-only confirmation. Zero requests occur, and no booking is claimed.
- Reduced-motion emulation disables transitions/animations. With JavaScript disabled, all five sections remain visible without horizontal overflow and a clear fallback notice is present.
- Inputs are labelled and dialogs have accessible titles. Mobile details remain within the viewport and scroll when necessary.

Machine evidence: `output/playwright/lean-results.json`.

## Unchanged comparison and server protections

The initial server/current-site verification remains applicable: the original homepage passed 1024/390px geometry and EN→ES→EN language switching. Its production form was tested only through the protected local server; the injected guard appeared and zero requests followed submission.

The preview server binds to loopback, blocks network connections and form actions via CSP, rejects write methods and foreign Host values, and serves only allowlisted public paths. Initial checks covered traversal, symlink escape and private-path rejection. Source files of the current homepage were not modified by these protections.

Earlier evidence: `output/playwright/current-baseline-results.json` and the archived initial QA record.
